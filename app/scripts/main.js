window.onload = function () {
	const doc = document.documentElement;
	const body = doc.body;
	const _head = doc.firstChild;
	const _body = doc.lastChild;
	const _head_ = doc.childNodes[0];
	const title = _head.firstChild;
	// alert(_head.parentNode === doc);
};
